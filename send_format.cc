#include <iostream>
#include <vector>
#include <string>

using namespace std;

#define MAX_SZ 100

class send_format{
    private:
        const int def_max = MAX_SZ;
        bool def_size_check(string msg);
        const string 
}